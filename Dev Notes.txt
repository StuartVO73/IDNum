const objToString = (E) => {
    return E
        ? Object.keys(E)
              .map((key) => {
                  return key + ": " + E[key];
              })
              .join(", ")
        : "--";
};

7301010000086

            //this.data = [];
            //console.log("Payload = " + objToString(payload));
            this.data = await getHolidays(payload);
            /*const data = await getHolidays(payload);
            for (let i of data) {
                let j = { ...i };
                //j.isoDate = j.isoDate.substring(0, 10);
                this.data.push(j);
            }
            let n = 0;
            for (let i of this.data) {
                console.log("Column data[" + (n++) + "]: " + objToString(i));
            }
            */

